# MERN MySQL CRUD Tasks - Backend

This is the backend part of the MERN MySQL CRUD Tasks application. It's built using Express.js and Node.js, with MySQL as the database.

## Features

- REST API for tasks
- Connects to MySQL database

## 1. Installation

- Clone the backend repository
- Install NPM packages
- Enter your MySQL information in `.env` file
- Start the server

## 2. Usage

The server runs on `http://localhost:5000`. It provides a REST API for creating, reading, updating, and deleting tasks.

## Credits

##### Inspiration from

<a href="https://github.com/fazt/">Fazt</a>